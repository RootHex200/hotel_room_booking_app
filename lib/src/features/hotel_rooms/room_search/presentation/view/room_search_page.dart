

import 'package:flutter/material.dart';

class RoomSearchPage extends StatelessWidget {
  const RoomSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Room Search page"),
    );
  }
}